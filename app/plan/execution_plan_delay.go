package plan

import "domain-stress/model"

//待开发
func ExecutionDelay(ips []string, concurrency uint64, totalNumber uint64, request *model.Request) []model.PlanRequest {
	var planRequests []model.PlanRequest
	//计算各个IP地址的时延

	//根据时延进行加权 生成执行计划

	return planRequests
}
